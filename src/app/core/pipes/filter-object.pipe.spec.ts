import { FilterObjectPipe } from './filter-object.pipe';

describe('FilterObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
