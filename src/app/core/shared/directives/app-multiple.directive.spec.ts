import { AppMultipleDirective } from './app-multiple.directive';

describe('AppMultipleDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMultipleDirective();
    expect(directive).toBeTruthy();
  });
});
